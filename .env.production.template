# PRODUCTION Environment Variables Template
# 
# IMPORTANT: 
# 1. DO NOT commit actual production secrets to git
# 2. Generate new secrets for production (don't reuse development keys)
# 3. Add these variables in Vercel Dashboard → Settings → Environment Variables
# 4. Set Environment to "Production" for each variable

# =============================================================================
# REDIS CONFIGURATION (Choose ONE option)
# =============================================================================

# Option 1: Upstash Redis (RECOMMENDED for Vercel)
UPSTASH_REDIS_REST_URL="https://your-region-xxxxx.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-rest-token-here"

# Option 2: Traditional Redis (Redis Cloud, AWS ElastiCache, etc.)
# REDIS_URL="rediss://default:password@your-redis-endpoint:6379"

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Security monitoring (configure email/webhook for alerts)
SECURITY_ALERT_EMAIL="security@yourdomain.com"
SECURITY_WEBHOOK_URL="https://your-monitoring-service.com/webhook"

# Enable production security features
ENABLE_HPKP="true"
NODE_ENV="production"

# =============================================================================
# AUTHENTICATION & SESSIONS
# =============================================================================

# NextAuth configuration
NEXTAUTH_SECRET="generate-32-char-secret-with-openssl-rand-hex-32"
NEXTAUTH_URL="https://yourdomain.com"

# Google OAuth (create production OAuth app)
GOOGLE_CLIENT_ID="your-prod-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-your-production-client-secret"

# =============================================================================
# DATABASE (Already configured from your .env.local)
# =============================================================================

DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
# ... other database variables from Neon/Vercel

# =============================================================================
# MERCADOPAGO PAYMENT PROCESSING
# =============================================================================

# MercadoPago PRODUCTION credentials
MERCADOPAGO_ACCESS_TOKEN="APP_USR-your-PRODUCTION-access-token"
MERCADOPAGO_PUBLIC_KEY="APP_USR-your-PRODUCTION-public-key"

# Webhook security (generate with: openssl rand -hex 32)
MERCADOPAGO_WEBHOOK_SECRET="your-secure-webhook-secret-32-chars-min"

# =============================================================================
# ADMIN CONFIGURATION
# =============================================================================

ADMIN_EMAIL="admin@yourdomain.com"

# =============================================================================
# OPTIONAL: Additional monitoring/services
# =============================================================================

# If using external monitoring services
# SENTRY_DSN="https://your-sentry-dsn"
# ANALYTICS_ID="your-analytics-id"
# STRIPE_SECRET_KEY="sk_live_..." (if using Stripe)

# =============================================================================
# DEPLOYMENT CHECKLIST FOR VERCEL:
# =============================================================================

# □ 1. Create Redis database (Upstash recommended)
# □ 2. Generate production NEXTAUTH_SECRET: openssl rand -hex 32  
# □ 3. Create Google OAuth production credentials
# □ 4. Get MercadoPago PRODUCTION keys (not sandbox)
# □ 5. Generate webhook secret: openssl rand -hex 32
# □ 6. Add all variables in Vercel Dashboard
# □ 7. Set environment to "Production" for each variable
# □ 8. Deploy and test rate limiting
# □ 9. Monitor logs for Redis connection success
# □ 10. Test security alerts (optional)